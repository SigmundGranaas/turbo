/** `account` feature slice — sign-in / register / Google auth + theme & units
 *  settings. Its panel is shown via the host panel mutex (`panels.open('account')`);
 *  the slice has no open-state of its own. */
export { AccountSettingsPanel } from './AccountSettingsPanel';
