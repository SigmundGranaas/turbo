{
  "ConnectionStrings": {
    "_comment": "Local default; override via ConnectionStrings__Places. The ingestion job (Turbo.Places.Ingestion) writes to the same database.",
    "Places": "Host=localhost;Port=5440;Database=places;Username=postgres;Password=yourpassword"
  },
  "Turbasen": {
    "_comment": "Nasjonal Turbase (ut.no/DNT) proxy. Supply the secret key via env Turbasen__ApiKey; the /api/places/ntb proxy returns empty until it is set.",
    "ApiKey": "",
    "BaseUrl": "https://api.nasjonalturbase.no",
    "ApiVersion": "v3"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
