{
  "ConnectionStrings": {
    "_comment": "Local default; override via ConnectionStrings__Places. The ingestion job (Turbo.Places.Ingestion) writes to the same database.",
    "Places": "Host=localhost;Port=5440;Database=places;Username=postgres;Password=yourpassword"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
