/// User-Agent string sent on every outgoing tile / API request.
///
/// Bump the version here when pubspec.yaml's `version:` field changes. The
/// two are coupled intentionally — tile servers use this string for traffic
/// attribution and the contact link should always reach the project.
const String kTurboUserAgent =
    'turbo_map_app/1.0.21 (+https://github.com/sigmundgranaas/turbo)';
