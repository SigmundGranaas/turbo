[package]
name = "turbo-route-model"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

# L1 — the routing engine's own data model: the *shapes* it reasons
# about and the domain types it produces. Traits and plain data only.
#
# Boundary invariant (module design §12.6): this crate has ZERO
# dependencies outside `std`. Nothing here may name a file, a tile, a
# projection, a coordinate reference system, a config format, or the
# word "Norway". If a dependency ever appears in this file that is not
# `serde`, the ports have leaked and the game-engine portability case
# is broken.

[dependencies]
