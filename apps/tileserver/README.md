# Tile server (placeholder)

Reserved for the upcoming custom tile server. Nothing here yet.

When this lands:

- Add Dockerfile and source under `apps/tileserver/`.
- Add compose service entries in `infra/compose/`.
- Add a workflow `.github/workflows/tileserver_*.yml` with `paths: ['apps/tileserver/**']`.
