namespace Turboapi.Activities.Packrafting.value;

/// <summary>International scale of river difficulty (I = flat / easy,
/// V = expert / extreme, VI = unrunnable).</summary>
public enum WaterGrade
{
    Flatwater = 0,
    I = 1,
    II = 2,
    III = 3,
    IV = 4,
    V = 5,
    VI = 6,
}

public enum SegmentKind
{
    Paddle = 0,
    Portage = 1,
}
