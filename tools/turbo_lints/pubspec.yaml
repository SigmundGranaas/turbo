name: turbo_lints
description: Project-local custom_lint rules for the turbo Flutter app.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  analyzer: ^8.0.0
  custom_lint_builder: ^0.8.1
